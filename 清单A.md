# 清单A：课题4《信号降噪与增强》从开始到结束执行清单

> 课题目标：围绕“维纳滤波、卡尔曼滤波、去噪自编码器、U-Net”完成图像去噪实验，对同一张添加高斯噪声的图像进行去噪并比较 PSNR/SSIM。

---

## 一、项目启动与目标确认

- [ ] 明确课题要求并写成 1 段“任务定义”（输入、方法、输出、评价指标）。
- [ ] 确认实验对象：单张图像还是多张图像（建议至少 1 张主图 + 3 张补充图，增强说服力）。
- [ ] 确定结果评价指标：PSNR、SSIM（可补充运行时间和参数量）。
- [ ] 规划课程设计交付物：代码仓库、实验报告、结果图表、答辩 PPT。

---

## 二、环境搭建与仓库初始化

- [ ] 选定开发环境（建议 Python 3.10+，PyTorch，OpenCV，scikit-image，matplotlib）。
- [ ] 创建虚拟环境并记录依赖（`requirements.txt`）。
- [ ] 设计目录结构（建议）：

```text
project/
  data/
    raw/
    noisy/
    denoised/
  src/
    traditional/
    deep/
    utils/
  configs/
  notebooks/
  results/
    figures/
    metrics/
  report/
```

- [ ] 在 `README.md` 中补充：项目简介、运行方式、实验流程、结果说明。
- [ ] 固定随机种子，保证实验可复现。

---

## 三、数据准备与噪声构造

- [ ] 选择原始干净图像（高质量、细节丰富，建议灰度图与彩色图各 1 张）。
- [ ] 统一图像尺寸（如 256×256 或 512×512）。
- [ ] 添加高斯噪声并固定噪声参数：
  - [ ] 均值 `mean = 0`
  - [ ] 方差 `sigma^2`（建议做 2~3 档，如 0.01/0.02/0.05）
- [ ] 保存 `clean`、`noisy` 图像，命名规范统一。
- [ ] 写脚本自动生成噪声样本，避免手工处理。

---

## 四、传统方法实现（基线）

### 4.1 维纳滤波（Wiener）
- [ ] 实现或调用库函数完成维纳滤波。
- [ ] 调节窗口大小等参数并记录最优配置。
- [ ] 输出去噪图及指标（PSNR/SSIM）。

### 4.2 卡尔曼滤波（Kalman）
- [ ] 明确图像卡尔曼建模方式（逐行/逐列/像素序列化）。
- [ ] 设置状态转移、观测模型、过程噪声与观测噪声参数。
- [ ] 完成卡尔曼去噪并保存结果图。
- [ ] 输出指标并记录参数敏感性。

---

## 五、深度学习方法实现

### 5.1 去噪自编码器（Denoising Autoencoder）
- [ ] 设计网络结构（Encoder-Decoder）。
- [ ] 定义损失函数（MSE 或 L1）。
- [ ] 划分训练/验证数据（即使是小样本，也要留验证集）。
- [ ] 训练模型并保存最优权重。
- [ ] 可视化训练曲线（loss vs epoch）。
- [ ] 推理并输出去噪结果与指标。

### 5.2 U-Net 去噪
- [ ] 实现或复用标准 U-Net 结构。
- [ ] 配置训练超参数（batch size、lr、epoch、optimizer）。
- [ ] 完成训练并保存模型。
- [ ] 在同一 noisy 输入上推理，输出结果图。
- [ ] 计算 PSNR/SSIM 并记录。

---

## 六、统一评估与对比分析

- [ ] 保证四种方法输入完全一致（同一 noisy 图像）。
- [ ] 统一指标计算函数，避免评估口径不一致。
- [ ] 生成总对比表：
  - [ ] 方法名称
  - [ ] PSNR
  - [ ] SSIM
  - [ ] 推理时间
- [ ] 绘制可视化拼图：`clean | noisy | wiener | kalman | DAE | U-Net`。
- [ ] 对细节区域做局部放大对比（边缘、纹理区域）。
- [ ] 写结论：谁在指标上最好、谁在速度/复杂度上更优、原因是什么。

---

## 七、报告撰写（课程设计文档）

- [ ] 摘要：1 段讲清任务、方法、结果。
- [ ] 引言：去噪背景与应用场景。
- [ ] 方法：传统算法 + 深度学习算法原理与公式。
- [ ] 实验设置：数据、噪声、训练参数、硬件环境。
- [ ] 结果与分析：表格 + 可视化 + 误差讨论。
- [ ] 结论与展望：优缺点与可改进方向（如 BM3D、DnCNN、扩散模型）。
- [ ] 参考文献规范化（学校指定格式）。

---

## 八、答辩准备

- [ ] 8~12 页 PPT（问题定义、方法流程、核心结果、结论）。
- [ ] 准备 1 页“为什么要比较这四种方法”的解释。
- [ ] 准备常见问答：
  - [ ] 为什么用 PSNR/SSIM？
  - [ ] 为什么 U-Net 优于传统方法（或相反）？
  - [ ] 卡尔曼参数怎么定？
- [ ] 本地演示脚本可一键运行（建议 `run_all.sh` 或 `python main.py`）。

---

## 九、最终验收前自检

- [ ] 代码可从零运行（新环境安装依赖后可复现）。
- [ ] 模型与结果文件路径正确，无硬编码绝对路径。
- [ ] 指标、图像、表格在报告中一一对应。
- [ ] 所有图表有标题、坐标、单位（如有）。
- [ ] Git 提交历史清晰，关键版本可回退。

---

## 十、建议时间安排（2 周示例）

- [ ] 第 1-2 天：环境搭建 + 数据与噪声脚本。
- [ ] 第 3-4 天：维纳/卡尔曼实现与调参。
- [ ] 第 5-8 天：DAE 与 U-Net 实现训练。
- [ ] 第 9-10 天：统一评测、作图、对比分析。
- [ ] 第 11-12 天：写报告初稿。
- [ ] 第 13 天：补实验与润色。
- [ ] 第 14 天：答辩 PPT 与最终检查。

---

## 附：最低可交付版本（保底线）

- [ ] 1 张干净图 + 1 张高斯噪声图。
- [ ] 4 种方法全部跑通并保存结果图。
- [ ] 输出 PSNR/SSIM 总表。
- [ ] 提交一份包含方法、结果、结论的完整报告。

